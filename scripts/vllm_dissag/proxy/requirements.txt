# Runtime deps for moriio_pd_proxy.py
#
# Both current serving images already have these (GLM e03 and DSV3/Hy3 0.26):
#   rocm/pytorch-private:vllm-recent-source-basem-acb0f1dc-aiter-e03fa6040
#   rocm/pytorch-private:vllm-recent-source-basem-20260815
# Install only when running the proxy outside those images:
#
#   pip install -r requirements.txt
#
# Matches check_deps() in moriio_pd_proxy.py. Do not add FastAPI/uvicorn.
# Quart+Hypercorn is the c=1..512 path (deep listen backlog; app.debug stays False).
# Tests (test_moriio_pd_proxy.py) are stdlib unittest; no extra test deps.

quart>=0.19.0
hypercorn>=0.16.0
aiohttp>=3.9.0
pyzmq>=25.0.0
msgpack>=1.0.0
